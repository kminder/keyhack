# KeyPeek

```
wget https://github.com/kminder/keypeek/releases/download/v1.0.1/keypeek-1.0.1.jar -O /tmp/keypeek.jar
```

```
java -jar /tmp/keypeek.jar -f <keystore-filename> -p <keystore-password>
```
